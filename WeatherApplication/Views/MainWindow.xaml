<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Jessebot.WeatherApp.ViewModels;assembly=Jessebot.WeatherApp"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="Jessebot.WeatherApp.Views.MainWindow"
        Icon="/Assets/avalonia-logo.ico"
        Title="Jessebot.WeatherApp">

    <Design.DataContext>
        <vm:MainWindowViewModel/>
    </Design.DataContext>

    <DockPanel>
        <Button DockPanel.Dock="Bottom"> Bottom Button </Button>
        <TextBlock DockPanel.Dock="Top" Text="Weather App" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <TextBlock Text="{Binding Greeting}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </DockPanel>

</Window>
